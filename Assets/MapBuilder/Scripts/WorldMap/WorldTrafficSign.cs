using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using static MapData;

public class WorldTrafficSign : MonoBehaviour
{
    [SerializeField] TrafficSignType signType;

    [Header("Debug")]
    [SerializeField] MapSignData signData;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
